from setuptools import setup, find_packages

setup(
    name='comms_core',
    version='0.1',
    description='Core comms package for Inspiration RobotX',
    author='Eesh Vij',
    packages=find_packages(include=['comms_core'])
)